import type { Metadata } from "next";

export const metadata: Metadata = { title: "Privacy", description: "How WebTaskKit handles local browser-tool inputs, bounded public-document SEO checks and limited technical data.", alternates: { canonical: "/privacy" } };
export default function PrivacyPage() {
  return <main className="legal-page section-wrap"><div className="breadcrumbs"><a href="/">Home</a><span>/</span><span>Privacy</span></div><p className="eyebrow">Plain-language policy</p><h1>Privacy at WebTaskKit</h1><p className="page-lead">Most tool content stays on your device; public-document SEO checks use narrow, transient server fetches.</p><div className="prose"><h2>Local tool inputs</h2><p>QR payloads, barcode values, text documents, images, PDFs, SVG source and generated tones are processed by code running in your browser. WebTaskKit does not need to upload that content to generate the result.</p><h2>Public-document SEO checks</h2><p>The page audit and robots.txt or sitemap validator send the public URL you explicitly submit to the WebTaskKit server so it can retrieve one HTML, text or XML document. An optional robots test path and crawler name are evaluated only for that request. Submitted values and fetched content are held in memory, are not stored and are not written to access, event or analytics logs. The validator does not automatically fetch declared sitemaps, child sitemaps or listed pages. The destination website can observe the server request. Do not submit private dashboards, signed download links or URLs containing access tokens.</p><h2>Local files and downloads</h2><p>When you open a file, your browser makes it available only to the tool in the active tab. Results are downloaded directly to your device. Closing or refreshing the tab clears unsaved work.</p><h2>Hosting data</h2><p>Like most websites, our hosting provider may process basic request information such as IP address, browser type and timestamps for security and reliable delivery. Our application access logs use only allowlisted canonical paths and deliberately omit IP addresses, query strings, referrers, cookies and user-agent details.</p><h2>Tool usage events</h2><p>When you interact with a tool, WebTaskKit may record a small first-party event containing only an allowlisted action name and that tool&apos;s canonical page path. The event record never contains your text, files, submitted URLs, fetched content, test paths, crawler names, generated values, monetary values, query strings or full referrer. The dedicated event log does not retain IP addresses, cookies, browser or device identifiers, fingerprints, or user-agent details. Privacy-minimized interaction records and necessary access logs are compressed and retained for up to 14 days, then deleted.</p><h2>Web analytics</h2><p>We use Cloudflare Web Analytics to understand aggregate page traffic and website performance. Cloudflare states that this service does not use cookies, local storage or fingerprinting, and that visitor IP addresses are not stored with analytics data. The analytics beacon receives page and browser performance information, not the text, files, submitted audit URLs, fetched documents, test paths, crawler names, codes, graphics or sounds you use inside our tools. WebTaskKit does not use advertising trackers or sell tool contents.</p><h2>Questions</h2><p>Until a public support address is added, you can use the site without creating an account or sharing contact information.</p><p className="legal-updated">Last updated: August 19, 2026.</p></div></main>;
}
