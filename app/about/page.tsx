import type { Metadata } from "next";

export const metadata: Metadata = { title: "About", description: "WebTaskKit builds fast, focused utilities with local processing where possible, guarded server tasks where necessary, and no signup.", alternates: { canonical: "/about" } };
export default function AboutPage() {
  return <main className="legal-page section-wrap"><div className="breadcrumbs"><a href="/">Home</a><span>/</span><span>About</span></div><p className="eyebrow">Why WebTaskKit exists</p><h1>Everyday web tasks should be straightforward.</h1><p className="page-lead">WebTaskKit is a focused collection of web utilities: quick to open, clear to use and respectful of your content.</p><div className="prose"><h2>Our product promise</h2><p>No mandatory account, no artificial waiting screen and no tool input sent to a server when modern browsers can do the job locally. When a task requires a server, its inputs, network access and retention are narrowly documented.</p><h2>Built for useful outcomes</h2><p>Every tool begins with a real working area, then explains the format, limitations and next step. Clean downloads and explainable checks matter more than a large directory count.</p><h2>What comes next</h2><p>We will improve these ten tools based on completion, reliability and genuine user needs before adding more. Browse the <a href="/#tools">current tools</a> and choose the one that gets your task done.</p></div></main>;
}
