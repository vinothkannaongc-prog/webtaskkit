import type { Metadata } from "next";

export const metadata: Metadata = { title: "About: Free Browser Tools With No Signup", description: "WebTaskKit builds fast, focused utilities with local processing where possible, guarded server tasks where necessary, and no signup.", alternates: { canonical: "/about" } };
export default function AboutPage() {
  return <main className="legal-page section-wrap"><div className="breadcrumbs"><a href="/">Home</a><span>/</span><span>About</span></div><p className="eyebrow">Why WebTaskKit exists</p><h1>Everyday web tasks should be straightforward.</h1><p className="page-lead">WebTaskKit is a focused collection of web utilities: quick to open, clear to use and respectful of your content.</p><div className="prose"><h2>Our product promise</h2><p>No mandatory account, no artificial waiting screen and no tool input sent to a server when modern browsers can do the job locally. When a task requires a server, its inputs, network access and retention are narrowly documented.</p><h2>The problem with most free online tools</h2><p>Search for any common file task and the results look identical: a landing page, an upload box, a progress bar that takes longer than the work requires, and a download gated behind an account or a queue. The upload is usually the point. Your file goes to a server you know nothing about, and the retention policy is either absent or written to be unreadable.</p><p>Almost none of that is technically necessary any more. Browsers can render PDFs, decode and re-encode images, generate barcodes and synthesise audio without contacting anyone. When a tool uploads your file anyway, that is a business decision, not a constraint.</p><h2>What runs where, exactly</h2><p>Eight of the ten tools process their working inputs entirely in your browser. QR payloads, barcode values, text documents, images, PDFs, SVG source and generated tones are processed by code in the tab you have open; that source content is not uploaded. Some tool code and PDF support assets load on demand, so the page may still need a connection while a tool starts. Refreshing or closing the tab clears unsaved work.</p><p>Two tools handle a different kind of task. The on-page SEO audit and the robots.txt and sitemap validator must retrieve an arbitrary public document, and browsers restrict those cross-origin reads. A guarded server fetch is therefore required. These tools retrieve one document per request, hold it in memory, never store it and never write it to logs. The validator will not follow declared sitemaps or crawl the pages it finds. Those boundaries are set out in full in our <a href="/privacy">privacy policy</a>.</p><h2>Built for useful outcomes</h2><p>Every tool begins with a real working area, then explains the format, limitations and next step. Clean downloads and explainable checks matter more than a large directory count.</p><p>The same principle governs what a tool tells you when it cannot help. A converter that silently produces a broken file is worse than one that says which input it rejects and why. An SEO check that returns a score without showing the evidence behind it is not a check, it is a guess with a number attached.</p><h2>No account required</h2><p>There is no signup because none of these tools requires a user account. No tool here has a usage limit to unlock, no result is watermarked, and no output is held back for a paid tier. Tool inputs are not attached to an account. The privacy-minimized first-party interaction records we keep contain only an allowlisted action name and canonical page path; they are retained for up to 14 days, then deleted.</p><h2>What comes next</h2><p>We will improve these ten tools based on completion, reliability and genuine user needs before adding more. A tool that handles its edge cases well earns a place; a directory of half-finished ones does not. Browse the <a href="/#tools">current tools</a> and choose the one that gets your task done.</p></div></main>;
}
